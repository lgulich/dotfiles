requires:
  - brew

install:
  ubuntu:
    - install_ubuntu.sh
  macos:
    - install_macos.sh
symlinks:
  config/yapf.style: ~/.yapf.style
  config/pylintrc: ~/.pylintrc
  config/clang-format: ~/.clang-format
  config/yamlfmt.yaml: ~/.yamlfmt
source:
  - helpers.sh
